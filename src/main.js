import App from './components/App.html';

var DrawingBox = new App({
  target: document.getElementById('drawing-box')
});

//Instantiate sibsidiary services
// State.init(app);

export default {DrawingBox};
